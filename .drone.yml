---

kind: pipeline
name: default
workspace:
  base: /drone
  path: src/${DRONE_REPO}

steps:
  - name: molecule
    image: lowess/drone-molecule:2.6.8
    pull: true
    settings:
      task: test
    volumes:
      - name: dockersock
        path: /var/run/docker.sock


volumes:
  - name: dockersock
    host:
      path: /var/run/docker.sock
